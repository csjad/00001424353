# cn-stock-desktop 运行依赖（完整版）
# Python >= 3.10（本项目在 3.13.14 上验证）

# 纯逻辑层（无 GUI）：撮合 / 回测 / 数据 / 持久化 / 规则
# 服务端 / CI / headless 只需装这个子文件即可
-r requirements-core.txt

# ---------- GUI（仅桌面客户端需要） ----------
PyQt6>=6.7.0
pyqtgraph>=0.13.7
