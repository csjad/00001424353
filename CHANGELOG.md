# 更新日志 / Changelog

本项目所有重要变更均记录于此文件。格式参考 [Keep a Changelog](https://keepachangelog.com/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [v1.0.0] — 2026-08-31

首个可发布版本。桌面直启的 A 股模拟交易终端，覆盖行情图表、模拟交易、量化回测，
**全程无社交功能**，离线可用（内置示例数据 + 离屏渲染自检）。

### ✨ 功能 (Features)
- **桌面直启形态**：双击 `A股模拟交易终端.exe` 即开，无需终端、无需服务、无需装 Python
  环境（PyInstaller 冻结，单一可执行文件）。
- **行情图表**：pyqtgraph K 线 + 成交量，支持日 K 与 5/15/30/60 分钟周期、前/后复权切换、
  多均线叠加（5/10/20/60）。
- **模拟交易**：买入/卖出、限价/市价、T+1 规则、整百手校验、涨跌停拒单、可用资金校验；
  持仓/委托/成交三张表实时渲染。
- **量化回测**：内置 6 套策略（买入持有基准、双均线交叉、RSI 超买超卖、布林带通道、
  MACD 背离、网格交易），输出收益/基准/夏普/回撤对比。
- **盘口与自选**：股票列表搜索、自选股增删、单票实时盘口刷新。
- **实验性单票实时快路径**（`realtime_per_symbol`，默认关闭、安全可回退）：开启后实时行情
  由「全市场快照 59 请求」降为「每只 1 请求」，异常自动回退，绝不比现状更差。

### 🐛 修复 (Bug Fixes)
- **快速切换自选股竞态**（用户反馈「其他示例股票打不开」）：`_load` 每次 new Worker，旧
  Worker 结束仍会把上一次标的数据写回当前图表/盘口，表现为串味或「打不开」。引入单调
  `_load_token`，`_on_data`/`_on_error` 写入前校验，过期结果一律丢弃；渲染异常改为界面内
  「渲染失败」提示而非静默吞掉。
- **实时行情缓存穿透**：消除实时行情重复触发的 59 次分页请求，并修正股票列表失败时的降级
  路径（主路径失败快速失败，不再触发 59 次分页）。
- **akshare 超时失效**：原 `timeout` 字段从未真正传给底层请求，现已生效。
- **UI 线程阻塞**：下单与定时刷新原本在 UI 线程同步联网，现改为后台线程，界面不再卡死。
- **离线体验三处缺陷**：状态栏「撒谎」（离线仍显示在线）、失败弹窗轰炸（每次自动刷新都弹
  模态框）、代码整洁度。
- **冻结后相对导入报错**：改用顶层 `launcher.py` 作为 PyInstaller 入口修复。
- **CMD codepage 解析炸**：打包与启动逻辑挪到 `build.py` / `run.py`，根治 `.bat` 多行中文 +
  续行在 GBK 下乱码问题。
- **backtest_view 缺失 `QColor` 导入**：补齐，避免回测视图崩溃。
- **图表渲染健壮性**：`CandlestickItem._prepare` 对 NaN/非数值行做防御性兜底，单根坏 K 线
  不再拖垮整张图。

### ⚡ 性能 (Performance)
- **打包体积优化**：exe 由 124.7 MB 降至 72.1 MB（**-42.2%**），启动更快、分发更轻。
- **全市场快照缓存**：TTL 内重复取数只触发 1 次真实请求（6 次取数省下 5×59 = 295 个 HTTP
  请求）；TTL 过期后正常刷新，缓存不永久失效。

### 🔧 构建 / CI (Build & CI)
- 新增 GitHub Actions：windows-latest 自动打包并发布 exe。
- 打包步骤改用 `python build.py`，避免 YAML 内嵌多行中文 + pyinstaller 参数出错。
- **打包前自检闸门**：`_verify.py`（27 项断言）+ `_smoke.py` 在本地与 CI 都会先跑，`build.py
  --skip-tests` 可跳过；PyInstaller 缺失时 `build.py` 自装。

### 🧪 测试 (Tests)
- 新增 `scripts/_verify.py` 回归测试：沉淀持久化往返（SQLite 写入→关闭→按 main.py 恢复序列
  重开→一致性比对）、交易视图渲染行数/单元格、离线失败冷却、快照缓存、股票列表降级开销、
  单票快路径、行情视图多标的加载（含 stale 丢弃）。
- `scripts/_smoke.py` 冒烟测试：撮合引擎（T+1/整百/涨跌停/资金校验）、6 策略回测、绩效指标。

### 📄 文档 (Docs)
- README 新增离屏零联网渲染截图与说明，同步配置项（`realtime_per_symbol`）与目录结构，
  修正 exe 体积数值。

---

## 提交明细（全部 16 条）

| 提交 | 类型 | 说明 |
|------|------|------|
| `aabd924` | fix | 修复快速切换自选股「其他示例股票打不开」竞态 |
| `26287d0` | docs | README 同步配置说明，修正 exe 体积与目录结构 |
| `4452ea2` | feat | 单票实时快路径（默认关闭，安全可回退） |
| `f170b93` | fix | 消除实时行情 59 次缓存穿透 + 股票列表降级路径 |
| `439919a` | perf | 打包体积 124.7→72.1 MB（-42.2%） |
| `31c334c` | ci | 打包前自检闸门，两套测试本地/CI 先跑 |
| `c72c019` | test | 新增 _verify.py 回归测试 |
| `8d085cc` | fix | akshare 请求超时真正生效 |
| `20ba3b5` | fix | 下单与定时刷新不再阻塞界面 |
| `6cb2591` | fix | 离线体验三处缺陷 |
| `3754729` | docs/fix | README 截图 + backtest_view 缺 QColor 导入 |
| `bba1ee9` | ci | Actions 改用 python build.py |
| `b78d1a5` | fix | 打包/启动逻辑挪到 build.py / run.py，根治 codepage |
| `e31e6a4` | ci | GitHub Actions 自动打包并发布 exe |
| `35f838f` | fix | 顶层 launcher.py 作 PyInstaller 入口，修复冻结相对导入 |
| `71e897c` | feat | Initial commit：A股模拟交易终端（桌面直启版） |

[v1.0.0]: https://github.com/<your-org>/cn-stock-desktop/releases/tag/v1.0.0
